# Continuous org-wide GitHub → Forgejo/Codeberg/GitLab mirror with issue and PR metadata sync

Live: https://forge-sync.sociobot.in — built by the Param Factory (`cli`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
